import * as React from 'react';
import * as classNames from 'classnames';
import './Navigation.scss';

export const Navigation = () => (
  <div className={'navigation'}>
    {/*<button className={classNames('btn', 'btn-primary')}>☰</button>*/}
  </div>
);
